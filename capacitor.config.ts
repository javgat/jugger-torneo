import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.javgat.jugger-torneo',
  appName: 'jugger-torneo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
