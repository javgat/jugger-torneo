import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.javgat.jugger_torneo',
  appName: 'jugger-torneo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
